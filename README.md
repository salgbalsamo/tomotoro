# tomotoro

Only works on Windows currently
Will open a tab in default web browser
Requires Spotify Premium to function (will prompt login on first run)
Requires recent Spotify playback on a device for program to play on that device (program can control Spotify on any device)

ONLY NEEDS TO BE RUN ONCE PER PC BOOT CYCLE